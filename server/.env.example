MONGO_URI=
JWT_SECRET=
GEMINI_API_KEY=
PORT=5000
UPLOAD_DIR=uploads

# Email verification (SMTP)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_FROM=
FRONTEND_URL=http://localhost:5173

# Optional: a PEM CA bundle for a trusted enterprise TLS-inspection proxy.
# Do not set EMAIL_TLS_REJECT_UNAUTHORIZED=false.
EMAIL_TLS_CA_PATH=
